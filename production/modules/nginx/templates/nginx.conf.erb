worker_processes auto;
events {
  worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name <%= @fqdn %>;

        root /var/www/html;
        index index.html;

        location / {
            try_files $uri $uri/ =404;
        }
    }
}
